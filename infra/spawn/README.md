# Spawn integration

Thin wrapper for [OpenRouterTeam/spawn](https://github.com/OpenRouterTeam/spawn). Install spawn CLI separately; do not vendor the full spawn monorepo.

- `zero-worker.json` — headless VM config
- `sh/hetzner/zero.sh` — build + systemd bootstrap
