# Demo Video Script (6 steps)

1. Show `docs/ARCHITECTURE.md` — Rust chain + TS orchestrator + hybrid router.
2. Terminal: `make build-all` and start chain + worker.
3. Log line: `[SENTINEL] PASS` and `[HYBRID ROUTER] x402`.
4. Log line: SAP discovery agent count.
5. Browser: `docs/VERIFIED.md` with real Solscan/Orb tx links.
6. Statement: competing in **both** categories — General Payment Volume (escrow) and Ace Data Cloud Usage (x402).
